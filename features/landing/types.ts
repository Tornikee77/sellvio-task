export type HeaderBtnsProps = {
  id: number;
  alt: string;
  src: string;
};

export type ProgressDashProps = {
  currentAmount: number;
  goalAmount: number;
};
export type ButtonProps = {
  text: string;
  color: string;
  size: string;
  img?: string;
};
